export default function HomePage() {
  return (
    <main style={{ fontFamily: "system-ui", padding: "2rem", maxWidth: 640 }}>
      <h1>IT Support platform</h1>
      <p>
        Phase 1 (tenant &amp; auth core) is in place. Admin dashboard, support
        portal, and the AI agent arrive in later phases — see{" "}
        <code>docs/ROADMAP.md</code>.
      </p>
    </main>
  );
}
