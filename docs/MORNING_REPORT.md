# Morning report — overnight autonomous run

> Template. The autonomous run fills this in before stopping. One report per run.

## Run metadata

- Date / run id:
- Working branch:
- Started / ended (and total duration):
- Tasks approved in scope:
- Final status: `completed all` | `stopped early (3 consecutive failures)` | `stopped (NEVER-action guard)`

## Summary at a glance

- Tasks completed: __ / __
- Tasks skipped: __
- Consecutive-failure stop triggered: yes / no
- Working tree clean at end: yes / no

## ✅ Tasks completed

| Task | Commit | Notes |
|------|--------|-------|
| | | |

## ⏭️ Tasks skipped (and why)

For each: task id, attempts made, the failure, and a pointer to the
`docs/REVIEW_NEEDED.md` entry.

| Task | Attempts | Reason / error (short) | REVIEW_NEEDED entry |
|------|:--:|------------------------|---------------------|
| | | | |

## 🧪 Verification status

- `npm run typecheck`: pass / fail (details)
- `npm run lint`: pass / fail (details)
- `npm run test`: pass / fail — N passed / M failed / K skipped
- Anything left red at end of run:

## ❓ Open questions for you

Decisions or clarifications blocking progress — especially anything that pushed a
task to `[needs-human]` or caused a skip.

-

## ▶️ Suggested next

- Recommended next tasks (incl. any `[needs-human]` ones now ready for a
  supervised session):
- Anything in `docs/REVIEW_NEEDED.md` to triage first:
